# RPMotos - Sua Loja de Motos Online

Este é o código inicial de uma loja de e-commerce focada na venda de motocicletas.

## 🛠️ Tecnologias Utilizadas

* **HTML5:** Estrutura da página.
* **CSS3:** Estilização (responsivo e moderno).
* **JavaScript (Vanilla):** Lógica para carregar os produtos dinamicamente.

## 🚀 Como Executar

1.  **Baixe** todos os arquivos (`index.html`, `style.css`, `script.js`).
2.  **Crie a pasta de Imagens:** Crie uma pasta chamada `assets` na raiz do projeto.
3.  **Adicione as Imagens:** Coloque as imagens das motos dentro da pasta `assets` com os nomes definidos em `script.js` (ex: `sportiva-r1000.jpg`).
4.  **Abra:** Dê um duplo clique no arquivo `index.html` para abrir no seu navegador.

## 💡 Próximos Passos (Desenvolvimento)

* **Carrinho de Compras:** Implementar a lógica completa do carrinho usando `localStorage`.
* **Detalhes do Produto:** Criar uma página dedicada para cada moto.
* **Filtros/Busca:** Adicionar funcionalidade para filtrar motos por marca, preço, etc.
* **Backend:** Integrar com um sistema de backend para processar pedidos, pagamentos (Stripe, PagSeguro, etc.) e gerenciar estoque.